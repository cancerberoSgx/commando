{
  "name": "commands-queue",
  "version": "0.0.3",
  "description": "execute command line in a queue",
  "main": "dist/src/index.js",
  "typings": "dist/src/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "test": "ts-node node_modules/jasmine/bin/jasmine",
    "prepare": "npm run build",
    "docs": "npm run build && npx typedoc --options typedoc.ts"
  },
  "author": "Sebastian Gurin",
  "license": "MIT",
  "devDependencies": {
    "@types/jasmine": "^2.8.8",
    "@types/node": "^10.9.4",
    "@types/p-queue": "^2.3.1",
    "@types/shelljs": "^0.8.0",
    "jasmine": "^3.2.0",
    "ts-node": "^7.0.1",
    "typedoc": "^0.12.0",
    "typescript": "^3.0.3"
  },
  "dependencies": {
    "p-queue": "^3.0.0",
    "shelljs": "^0.8.2"
  }
}
